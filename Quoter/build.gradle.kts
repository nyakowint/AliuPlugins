version = "1.1.4"
description = "Re-adds the famous quote button in the message context menu. **May not work for some people/devices**"

aliucord {
    changelog.set("""
        # 1.1.4
        * Enable/disable adding user mention
        * You can now choose whether to replace or add to what's currently in the chat box
        # 1.1.3
        * Set the view's font
    """.trimIndent())
}