version = "1.1.1"
description = "Re-adds the famous quote button in the message context menu."

aliucord {
    changelog.set("""- quoted messages containing newlines should properly format now
     - uhhhhhh hopefully fix null pointer that i just cant reproduce myself smfh
    """.trimIndent())
}