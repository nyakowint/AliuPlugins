version = "1.0.7"
description = "Changes grammatically correct sentences to discord speak"

aliucord {
    changelog.set("""
        # 1.0.6
            * Update to 101.3
        # 1.0.5
            * Entirely ignores messages with links to prevent dumb shit like invites breaking
        # 1.0.4
            * Setting to ignore bots
    """.trimIndent())
}