version = "1.0.3"
description = "Changes grammatically correct sentences to discord speak"

aliucord {
    changelog.set("""
        # 1.0.3
            * Ignores messages with plugin links
        # 1.0.2
            * Everything is lowercase now. ignore yourself to send other stuff
        # 1.0.1
		    * invert boolean
    """.trimIndent())
}