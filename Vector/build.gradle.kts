version = "1.0.1"
description = "makes every profile picture vector"

aliucord {
    changelog.set("""
        * vector
    """.trimIndent())
    updateUrl.set("https://raw.githubusercontent.com/aliucoin/coinplugins/builds/updater.json")
    buildUrl.set("https://raw.githubusercontent.com/aliucoin/coinplugins/builds/Vector.zip")
}